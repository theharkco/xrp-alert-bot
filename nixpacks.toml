[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[phases.start]
cmds = ["python3 -m uvicorn src.api:app --host 0.0.0.0 --port $PORT"]