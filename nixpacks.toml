[phases.setup]
nixPkgs = ["python311", "uv"]

[phases.install]
cmds = ["uv pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[phases.start]
cmds = ["uv run uvicorn src.api:app --host 0.0.0.0 --port $PORT"]